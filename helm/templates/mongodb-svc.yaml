apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-mongodb
  labels:
    app: mongodb
spec:
  clusterIP: None 
  ports:
    - port: 27017
      targetPort: mongodb
      name: mongodb
  selector:
    app: mongodb

{{- if .Values.rocketchat.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Release.Name }}-rocketchat-uploads
  labels:
    app: rocketchat
spec:
  storageClassName: {{ .Values.rocketchat.persistence.storageClassName | quote }}
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
{{- end }}
